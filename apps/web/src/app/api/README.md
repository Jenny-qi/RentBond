# API 路由

负责人 D。Node.js catch-all 路由由 [...path]/route.ts 接入服务模块，契约见 [API 文档](../../../../../docs/interfaces/api.md)。

所有私有资源每次服务端校验租约/案件 ACL。邀请仅能查找和关联，不能替代签约。

请求、响应、错误、版本和金额规则已在 API 文档冻结供 C 消费。写接口不能接受客户端提交的付款成功状态。
