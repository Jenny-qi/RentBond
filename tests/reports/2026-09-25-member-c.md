# 成员 C 本地验证记录

日期：2026-09-25。执行者：Codex；源码基线 `78e8cd4406e5a4d6b5a0324834f535c1efc23f49` 加未提交修改。网络：仅本地浏览器虚构模型；未部署、未发交易、未操作真实 passkey。

## 前次检查

- 初始 `node scripts/check-scaffold.mjs` 失败：Web README 引用了不存在的 `docs/member-c-handoff.md`。已补文件，待最终重跑。
- 前端模型原有 6 项测试；补充后曾运行 13 项，13/13 通过。
- 前次 `npm run typecheck --prefix apps/web` 与 `npm run build --prefix apps/web` 成功；后续修改需再跑。
- 前次本地浏览器检查：模拟租客登录；700 领取取消时余额不变；明确确认后可领从 700 变为 0、累计已领变为 700；加载未入金后各余额为 0。没有真实链上交易。
- 浏览器发现状态条可能遮挡页面底部按钮，增加底部空间；最新 360px 视口检查结果见下。

## 最终检查

环境：Windows PowerShell、Node v24.14.0、Next 15.5.9、React 19.1.4、TypeScript 5.9.2、Mera 0.2.0、viem 2.56.9。

| 命令 / 检查 | 实际结果 | 限制 |
| --- | --- | --- |
| `npm run test --prefix apps/web` | Passed，21 tests / 21 pass / 0 fail | 前端模型 17 项、SDK 适配 4 项；WebAuthn 使用替身，签名为公开测试向量 |
| `npm run typecheck --prefix apps/web` | Passed，退出码 0 | 仅 Web 包 TypeScript |
| `npm run build --prefix apps/web` | Passed，退出码 0；全部 P01–P12 路由编译，8 个静态页面生成 | 未部署；API 尚未实现 |
| `node scripts/doctor.mjs` | Passed，Node/workspace/env template/requirements 四项 OK | 明确 SCAFFOLD ONLY |
| `node scripts/check-scaffold.mjs` | Passed，159 files，34 FR / 16 SC / 52 AT / 5 TS / 14 RB，原文档断链已修复 | 不验证外部 URL、真实链或业务 |
| `git diff --check` | Passed，退出码 0 | 未提交；检查不替代独立评审 |
| `npm run start --prefix apps/web` | Passed，本地 3000 Ready | 用于本轮生产构建 UI 检查 |

浏览器复核使用本地生产构建：

1. 登录页可见独立“真实 Mera 账户试验”；未填写原地址/未勾选新建说明时对应按钮禁用。未点击真实凭证创建或恢复，未操作用户认证器。
2. 360×800 下登录页和结算页 document scrollWidth=345、innerWidth=360；无页面级横向溢出，导航自身横向滚动。
3. Alice 初始 700/100/200 → 显式加载 hardEndAt → 点击最终退出 → 确认对话框说明保留有效主结果 → 模拟确认后 P12 显示租客可领 850、房东可领 150、U=0，累计已领均为 0。证明分配未冒充领取。
4. 本轮浏览器捕获的 error 日志为空；视口已恢复。原服务器未启动时首次打开曾 ERR_CONNECTION_REFUSED，启动生产服务后复核成功。

新增 SDK 安装首次因 `ox` 下载 ECONNRESET 失败，重试后成功安装 23 个包；没有更换来源、版本或跳过类型检查。

未运行：真实 TS05、SIWE、Gas 补给、私有 ACL、文件提交、CHECKOUT 完整联调、真实链上 approve/fund/withdraw、交易替换、导出、跨设备/停运恢复、独立 clone 和独立审查。完整范围与未完成项见 [成员 C 交接](../../docs/member-c-handoff.md)。本报告不构成任何 AT/TS 完整通过或独立审计。
